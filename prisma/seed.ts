// Prisma seed — populated in Phase 14.
// For now this is a no-op so `prisma migrate dev` doesn't fail.

async function main() {
  console.log("[seed] no fixtures yet — populated in Phase 14");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
